import { Metadata } from 'next'

export const metadata: Metadata = {
    title: 'HS :: Login',
    description: 'Bblog login page',
}

const Login = () => {
    return <>login</>
}

export default Login
